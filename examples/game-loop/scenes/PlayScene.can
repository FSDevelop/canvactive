<script scene>
import Player from "../entities/Player";
import Hud from "../ui/Hud.can";
</script>

<update>
(context) => {
  Player.update(context);
}
</update>

<create>
({ draw }) => {
  draw(Hud);
  draw(Player);
}
</create>
